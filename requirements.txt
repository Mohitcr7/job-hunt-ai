# Job Hunt AI — dependencies
# Install with: pip install -r requirements.txt
# Then:         playwright install chromium

# --- LLM & agent framework ---
langchain>=1.0
langchain-core>=1.0
langchain-google-genai>=4.0     # Gemini (default provider, free tier available)
langchain-openai>=1.0           # optional alternative provider
langgraph>=1.0

# --- Resume parsing ---
pdfplumber>=0.11
pydantic>=2.7

# --- Job matching (local embeddings, free) ---
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26

# --- Job scraping ---
python-jobspy>=1.1              # LinkedIn + Indeed (reliable, specialised client)
scrapling[fetchers]>=0.4       # Naukri + company pages: JS render + self-healing selectors
                               # after install, run:  scrapling install   (downloads the browser)
pandas>=2.0

# --- Web dashboard ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9         # file uploads

# --- Notifications (optional — fail soft if unconfigured) ---
slack-sdk>=3.27
sendgrid>=6.11

# --- Utilities ---
python-dotenv>=1.0
loguru>=0.7
requests>=2.31              # OpenRouter /rerank calls
openpyxl>=3.1                # writes the daily job spreadsheet
